services:
  postgres:
    image: postgres:14-alpine
    container_name: snake-game-postgres
    environment:
      POSTGRES_USER: ${POSTGRES_USER:-postgres}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-postgres}
      POSTGRES_DB: ${POSTGRES_DB:-snake_game}
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER:-postgres}"]
      interval: 5s
      timeout: 5s
      retries: 5
    networks:
      - snake-game-network

  backend:
    build:
      context: ./snake-game-be
      dockerfile: Dockerfile
    container_name: snake-game-backend
    volumes:
      - ./snake-game-be:/app
    environment:
      DATABASE_URL: postgresql://${POSTGRES_USER:-postgres}:${POSTGRES_PASSWORD:-postgres}@postgres:5432/${POSTGRES_DB:-snake_game}
      SECRET_KEY: ${SECRET_KEY:-your-secret-key-here-change-in-production}
      ACCESS_TOKEN_EXPIRE_MINUTES: ${ACCESS_TOKEN_EXPIRE_MINUTES:-1440}
      SESSION_TIMEOUT: ${SESSION_TIMEOUT:-300}
    ports:
      - "8000:8000"
    depends_on:
      postgres:
        condition: service_healthy
    command: ["uvicorn", "app.main:app", "--host", "0.0.0.0", "--port", "8000", "--reload"]
    networks:
      - snake-game-network

  frontend:
    build:
      context: ./snake-game-fe
      dockerfile: Dockerfile
    container_name: snake-game-frontend
    volumes:
      - ./snake-game-fe:/app
      - frontend_node_modules:/app/node_modules
    environment:
      VITE_API_URL: ${VITE_API_URL:-http://localhost:8000/api/v1}
    ports:
      - "5173:5173"
    depends_on:
      - backend
    command: ["npm", "run", "dev", "--", "--host", "0.0.0.0"]
    networks:
      - snake-game-network

volumes:
  postgres_data:
  frontend_node_modules:

networks:
  snake-game-network:
    driver: bridge

